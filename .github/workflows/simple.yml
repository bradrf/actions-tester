name: simple
on:
  push:
    branches:
      - "main"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo "name=hello-${RANDOM}" >> $GITHUB_OUTPUT
        id: tag

      - run: echo "barf sees ref=${{ github.ref }} name=${{ github.ref_name }}"

      - run: env

      - name: Get Pull Request Number
        id: pr
        run: echo "num=$(gh pr view --json number -q .number || echo "")" >> $GITHUB_OUTPUT
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - run: "barf num? ${{ steps.pr.outputs.num }}"

      # - run: git tag "${{ steps.tag.outputs.name }}"
      # - run: git push --tags
      # - run: gh release create "${{ steps.tag.outputs.name }}"
      #   env:
      #     GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
